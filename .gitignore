# Rust
/target
**/target

# Node
node_modules
npm-debug.log*
yarn-error.log*

# Vite build output (regenerated by `npm run build`)
/dist

# Tauri generated
/src-tauri/gen
/src-tauri/target

# Local screenshots used while authoring (not part of the app)
.screenshot-*.png

# Env / secrets
.env
.env.*
.mcp.local.json

# Editor / OS
.DS_Store
.idea/
.vscode/

# 构建产物不进版本库。
#
# dist-web/ 曾经被提交进来（17MB，113 个文件），再手工 rsync 上线 —— 结果产物比源码落后
# 了 11 天而没人察觉，生产上跑的就是那份陈旧代码。构建产物一旦进 git，它和源码就会各走
# 各的。现在统一走 scripts/deploy-web.sh：每次从当前源码重新构建、校验、才允许发布。
/dist-web

# Local Claude settings backups — these mirror settings.local.json and can contain
# API tokens (a Figma token was found in one). Ignored for the same reason as the original.
.claude/settings.local.json.bak-*
