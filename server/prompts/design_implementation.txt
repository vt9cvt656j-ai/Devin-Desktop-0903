# michael-design 实现入口

这段只在用户要创建或修改真实 UI 时出现。实现必须以当前项目源码、现有设计语言、已采用的 michael-design 蓝本和核心层确定的语义令牌为准；只改请求涉及的范围，不因加载了设计规则就擅自重做整个产品。

先读相关入口、组件、样式、token 和素材，再实现完整状态与真实交互。具体规则由本轮按需组合的组件、脚手架、内容媒体、数据、动效、工程和验证模块提供；未加载的重型模块不代表可以臆造其要求，需要时用 `knowledge_search(domain="michael-design")` 或 `search_tools` 补取。
