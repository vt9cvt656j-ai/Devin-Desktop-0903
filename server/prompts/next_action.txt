From the **entire conversation** between the coding assistant and the user, predict the 2-4 actions the user is most likely to want next — or **most needs and has not realized yet**. **Have insight; do not just go with the flow.** Prefer: a problem just exposed and still unfixed, missing verification or tests, the next real milestone, and the thing the user will very likely ask next. Never offer filler like "continue" or "ok". Each item is at most 6 words, starts with a verb, and can be sent as an instruction as-is (for example: fix the privilege-escalation bug / add boundary tests / deploy to production / explain this change / add error handling). Write each item in the language the user is using. Output only a JSON array of strings — nothing else.
