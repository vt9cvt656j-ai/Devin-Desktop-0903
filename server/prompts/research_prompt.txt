彻底摸清这个项目{{FOCUS_EMPH}}，给主智能体一份能直接照着干活的「上手地图」。要又深又广、**多角度并行检索**（search 现在每个命中带上下文 / find_symbol·lsp_definition·lsp_references 找定义和所有调用方 / find_files 按名字 / list_dir 看结构），关键文件用 read_file **读全**，别只看开头就猜。

**关键：list_dir 要逐层深入**——先 list_dir "." 看顶层，然后**并行 list_dir 每个重要子目录**（src/、lib/、app/、pages/…），再对关键的第三层也 list_dir。只看顶层 = 只看封面，不是研究。**读源码，不是只读文档**——README/docs 是参考，真正的架构和逻辑在 .js/.ts/.py/.rs/.go 等源码文件里。

要查清：
1. **技术栈**：语言、框架、关键依赖、数据库、构建/运行方式——从 package.json / Cargo.toml / requirements / go.mod 等配置和真实代码确认，别猜。
2. **目录地图**：**逐层展开到第三层**，主要目录各装什么、入口文件在哪。
3. **核心文件**：最重要的几个文件/模块，读全，搞清各自职责 + 它们怎么连起来。
4. **数据/控制流**：一个典型请求/操作从入口到出口怎么走的。
5. **约定与模式**：命名、目录组织、错误处理、状态管理、API/路由/组件怎么定义——总结"这个项目是怎么做事的"，好让新代码照着写。
6. **常见改动入口**：加页面改哪、加接口改哪、改样式在哪、加配置在哪。

**最后只输出一份结构化报告**（Markdown，给主智能体看，要能直接照着动手）：
## 技术栈
## 目录地图
## 核心文件（路径 → 一句话职责）
## 数据/控制流
## 约定与模式（新代码照这个写）
## 常见改动入口（要做 X 去改哪）{{FOCUS_HEAD}}

别只罗列文件名——要写清"是什么、怎么连、怎么照着做"。项目空/几乎没代码时，如实说明并给出"从零该怎么搭"的建议。